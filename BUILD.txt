To setup for the first time:
cd engine
mkdir build
cd build
cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON ..

To build:
cd engine
cmake --build build

To setup:
go to settings.hpp to change the configuration

To run:

cd engine
./bin/ChessEngine

or
cd engine
./bin/Win3D <meshPath> <width> <height> <renderType>

or
cd engine
./bin/Win3D <scene> <renderType>

where:
<scene> is the ID of a hardcoded scene form the following: bunny, triangle, objects
<meshPath> is the filepath of the mesh to be rendered in the resources folder ignoring ".obj". e.g.: bunny_14404
<width> and <height> are the width and height of the screen
<renderType> is the pipeline to be rendered out of: "-t" for ray tracing and "-r" for rasterization
